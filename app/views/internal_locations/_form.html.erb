<%= form_for(@internal_location) do |f| %>
  <% if @internal_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internal_location.errors.count, "error") %> prohibited this internal_location from being saved:</h2>

      <ul>
      <% @internal_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="row">

    <%= f.label :activated %><br>
    <%= f.check_box :activated %>
  </div>

  <div class="row center-align">
    <!-- <%= f.submit %> -->
    <button type="submit" class="waves-effect waves-light btn btn-large"><i class="material-icons">input</i>Send</button>

  </div

<% end %>
