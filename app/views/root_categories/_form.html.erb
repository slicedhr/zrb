<%= form_for(@root_category) do |f| %>
  <% if @root_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@root_category.errors.count, "error") %> prohibited this root_category from being saved:</h2>

      <ul>
      <% @root_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
